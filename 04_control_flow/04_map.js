// MAP : contains unique values 
// Key , value pair

const map = new Map()
map.set(1, 'Neha')
map.set(2, 'Alok')
map.set(3, 'Suraj')
map.set(4, 'Siya')

for (const [key, value] of map) {
    console.log(key)
    
}